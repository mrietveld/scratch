package org.ocram.transaction.manager;

import org.springframework.transaction.jta.JtaTransactionManager;

public class TransManagerExamples {

    JtaTransactionManager txm;
    private void lookAtSource(org.springframework.transaction.jta.JtaTransactionManager txm ) { 
        int i = 0;
    }
}
