maxPoolSize=16
allowLocalTransactions=true
# JDBC/Database properties that are set in the maven pom
#
#   the below variable names (i.e. "bitronix.tm.resource.jdbc.lrc.LrcXADataSource) are
# automagically replaced with their values (defined in the pom.xml)
# because of the fact that <filtering> is set to true in for the
# src/test/resources directory in the pom.
#
className=bitronix.tm.resource.jdbc.lrc.LrcXADataSource
driverClassName=org.mariadb.jdbc.Driver
user=jbpm
password=jbpm
url=jdbc:mysql://localhost:3306/jbpm
serverName=
portNumber=3306
databaseName=jbpm
defaultSchema=public
dbBaseDir=/home/mrietvel/Workspace/jbpm_5/jbpm-marco/jbpm-persistence-jpa/target
makeBaseDb=false
testMarshalling=false