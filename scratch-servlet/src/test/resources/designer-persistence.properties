# options: jpa, jdbc, modeshape, guvnor
# jdbc: for large asset files, using direct jdbc means that the asset content 
#       is not loaded into memory, which means better performance. 
persistenceStrategy=jpa
# custom persistence settings
 